# Copita — core runtime dependencies.
# Install with:  pip install -r requirements.txt

aiohttp>=3.9
certifi>=2024.2.2
cryptography>=42.0
curl_cffi>=0.7
fastapi>=0.110
httpx>=0.27
pillow>=10.2
pydantic>=2.6
reportlab>=4.1
uvicorn>=0.29
yt-dlp>=2024.8

# --- Optional ---------------------------------------------------------------
# Magnet / torrent downloads. Ships as a wheel for most platforms:
#   pip install libtorrent
#
# Headless-browser fallback for a handful of JavaScript-heavy pages:
#   pip install playwright && playwright install chromium
